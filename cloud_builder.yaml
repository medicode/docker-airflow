timeout: 3600s
steps:
- id: airflow-two
  name: 'gcr.io/cloud-builders/docker'
  args: ["build", "-t", "us.gcr.io/$PROJECT_ID/docker-airflow-two:airflow-3.8", "."]
  dir: airflow_two/
  waitFor: ['-']

logsBucket: fathom-containers-cloud-build-logs-36tugmiz

images:
  - us.gcr.io/$PROJECT_ID/docker-airflow-two:airflow-3.8
